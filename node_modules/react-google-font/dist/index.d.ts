import PropTypes from 'prop-types';
interface Font {
    font: string;
    weights?: (string | number)[];
}
interface GoogleFontLoaderProps {
    fonts: Font[];
    subsets?: string[];
    display?: 'auto' | 'block' | 'swap' | 'fallback' | 'optional';
    text?: string;
}
declare function GoogleFontLoader({ fonts, subsets, display, text }: GoogleFontLoaderProps): null;
declare namespace GoogleFontLoader {
    var propTypes: {
        fonts: PropTypes.Validator<PropTypes.InferProps<{
            font: PropTypes.Validator<string>;
            weights: PropTypes.Requireable<(string | number)[]>;
        }>[]>;
        subsets: PropTypes.Requireable<string[]>;
        display: PropTypes.Requireable<string>;
        text: PropTypes.Requireable<string>;
    };
}
export default GoogleFontLoader;
